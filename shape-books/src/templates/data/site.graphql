query {
  settings: getSiteSettings {
    siteTitle
  }
  books: getBookList {
    total
    items {
      _contentTypeName
      _enabledAt
      title
      author {
        name
      }
      coverImage {
        path
      }
    }
  }
}
